version: v0.1
testName: SampleAppTest
testPlan: SampleApp.jmx
description: 'SampleApp Test Run'
engineInstances: 1
failureCriteria: 
    - avg(response_time_ms) > 5000
    - percentage(error) > 20
